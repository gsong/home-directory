<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>local.file-sweep</string>

	<key>ProgramArguments</key>
	<array>
		<string>/Users/george/bin/file-sweep</string>
	</array>

	<!-- Once a day at 09:00. If the Mac is asleep or off at that hour, launchd
	     runs the missed job at the next wake, so no RunAtLoad is needed. These
	     are age-based sweeps with a one-week floor: a run that lands hours late
	     changes nothing. -->
	<key>StartCalendarInterval</key>
	<dict>
		<key>Hour</key>
		<integer>9</integer>
		<key>Minute</key>
		<integer>0</integer>
	</dict>

	<!-- Deliberately NOT WatchPaths on ~/Downloads. The sweep moves files into
	     ~/Downloads/Archive, which is a write inside the watched directory, so
	     the job would retrigger itself on every run. Nothing here reacts to a
	     file arriving anyway: "older than a week" has no arrival to hang on. -->

	<!-- The script needs Full Disk Access, granted to /bin/bash in System
	     Settings, Privacy & Security. launchd runs it outside any terminal, so
	     it has none of the terminal's permissions. Without the grant, tccd
	     refuses find's requests for ~/Downloads and the cloud folders without
	     a prompt, because bash is an Apple binary. Every listing then comes back
	     empty, the loops run zero times, and the job exits 0 having logged
	     nothing. That is what happened on the first run, 2026-09-20. -->
	<key>ProcessType</key>
	<string>Background</string>
</dict>
</plist>
