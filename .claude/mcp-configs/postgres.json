{
  "mcpServers": {
    "postgres": {
      "command": "uvx",
      "args": [
        "--python",
        "3.12",
        "postgres-mcp",
        "--access-mode=unrestricted"
      ],
      "env": {
        "DATABASE_URI": "${DATABASE_URL}"
      }
    }
  }
}
